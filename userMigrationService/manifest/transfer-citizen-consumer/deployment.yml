apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-migration-citizen-consumer-service
  namespace: nsync
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-migration-citizen-consumer-service
  template:
    metadata:
      labels:
        app: user-migration-citizen-consumer-service
    spec:
      containers:
      - name: user-migration-citizen-consumer-container
        image: juanda0/user-migration-citizen-consumer-service:1.5
        ports:
        - containerPort: 5672
        env:
        - name: RABBITMQ_HOST
          valueFrom:
            configMapKeyRef:
              name: rabbitmq-config
              key: RABBITMQ_HOST
        - name: RABBITMQ_PORT
          valueFrom:
            configMapKeyRef:
              name: rabbitmq-config
              key: RABBITMQ_PORT
        - name: RABBITMQ_USER
          valueFrom:
            configMapKeyRef:
              name: rabbitmq-config
              key: RABBITMQ_USER
        - name: RABBITMQ_PASSWORD
          valueFrom:
            configMapKeyRef:
              name: rabbitmq-config
              key: RABBITMQ_PASSWORD
